import { TouchableOpacity } from "react-native-web";
import Texto from "../../componentes/Texto";

export default function(botao){
    compraBotao = <TouchableOpacity>
        <Texto>{botao}</Texto>
        </TouchableOpacity>
}